{
  "name": "learnpy",
  "version": "1.0.0",
  "scripts": {
    "start:admin": "concurrently \"cd admin && npm run dev\" \"cd backend && python app.py\"",
    "start:user": "concurrently \"cd user && npm run dev\" \"cd backend && python app.py\"",
    "start:all": "concurrently \"cd admin && npm run dev\" \"cd user && npm run dev\" \"cd backend && python app.py\"",
    "install:backend": "pip install -r backend/requirements.txt",
    "install:frontend": "npm install --prefix admin && npm install --prefix user"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^5.1.0",
    "tree-sitter": "^0.22.4",
    "tree-sitter-python": "^0.23.6"
  }
}
